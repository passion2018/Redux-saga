import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  color: {
    primary: '#FBC02D',
    secondary: '#512DA8',
    error: '#0097A7'
  },
  fontface: {
    fontFamily: 'Roboto',
  },
  shap: {
    textColor: "#FFFFF",
  }
});
export default theme;