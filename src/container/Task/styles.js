import theme from "../../commons/Theme";

const styles = () => {
  return ({
    box: {
      color: 'white',
      background: theme.color.secondary,
      padding: 20,
      margin: 20,
      fontFamily: theme.fontface.fontFamily
    }
  })
};

export default styles;